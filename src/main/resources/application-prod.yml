spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?user=postgres.abzhiywyipxpxgvbgvvx&password=${DB_PASSWORD}&sslmode=require
    username: postgres.abzhiywyipxpxgvbgvvx
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
  sql:
    init:
      mode: never

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000

ml:
  service:
    url: ${ML_SERVICE_URL}

security:
  max-failed-attempts: 5
  risk-threshold:
    medium: 30
    high: 70

server:
  port: ${PORT:8080}